# Utilities

Shared helper utilities belong under `pkg/utils` when they are reused across
multiple modules. The first Rust milestone does not need a shared utility crate,
so this directory is intentionally documentation-only for now.
